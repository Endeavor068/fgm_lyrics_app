/// Public URLs used in the app (store listings, legal pages, etc.).
class AppLinks {
  AppLinks._();

  /// Hosted privacy policy — must match the URL in Google Play Console.
  static const String privacyPolicy =
      'https://sites.google.com/view/fgmn-hymnal-privacy-policy';
}
